# Golden Trade X — Python dependencies
# Acotadas con ~= (compatible release) para builds reproducibles;
# Dependabot propone las actualizaciones de versión semanalmente.
#
# Core (always required)
MetaTrader5~=5.0
requests~=2.34

# Machine learning (scripts/ml_pipeline.py)
xgboost~=3.3
scikit-learn~=1.4
xgboost~=2.0
scikit-learn~=1.9

# Market data (scripts/correlation_engine.py)
yfinance~=0.2

# Live monitoring (scripts/live_monitor.py) — optional
python-dotenv~=1.0

# Testing
pytest~=8.0
pytest-cov~=7.1
