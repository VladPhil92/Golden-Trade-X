# Golden Trade X — exact direct dependency lock for official validation campaigns.
# Regenerate deliberately; never widen these constraints inside a frozen campaign.
requests==2.34.2
MetaTrader5==5.0.6147; platform_system == "Windows"
xgboost==3.4.1
scikit-learn==1.9.0
yfinance==0.2.66
python-dotenv==1.2.3
pytest==9.1.1
pytest-cov==7.1.0
